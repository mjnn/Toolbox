---
description: ECS 默认连接约定（root@47.116.180.173）
alwaysApply: true
---

# ECS 快速连接约定

- 当用户提到“连接 ECS / 登录 ECS / 连服务器（默认 ECS）”时，默认连接目标为：
  - Host: `47.116.180.173`
  - User: `root`
- 已配置 SSH 别名时优先使用（示例别名 **`ecs-main`**；以本机 `~/.ssh/config` 为准）：
  - `ssh ecs-main`
- 优先使用免密方式发起连接并执行验证命令：
  - `ssh ecs-main "hostname && uname -a"`
- 若别名不可用，使用你环境中已配置的私钥路径，例如：
  - `ssh -i <path-to-private-key> root@47.116.180.173`
- 禁止在规则中存储或回显明文密码。
- 若连接失败再提示用户提供临时认证信息，不主动要求长期保存密码。
- 默认进行连接后验证，执行：
  - `hostname && uname -a`
- 若用户明确要求交互式 shell，则执行：
  - `ssh ecs-main`
- 连接成功后需返回关键结果（主机名与系统信息），确认 ECS 已可用。
- 若连接失败，先自查网络/认证/端口原因并给出下一步修复建议。

