---
description: ECS 外网发布标准流程（Service ID 单工具版）
alwaysApply: true
---

# ECS 外网发布流程（固定执行）

- **与内网便携包解耦**：Windows 便携包由 `scripts/build-release.ps1` 在打包机生成，**不**纳入 ECS 镜像；**不要**把便携包目录写进 `Dockerfile` 或容器发布步骤。
- 当用户说“部署到ECS / 发布外网版 / 推到阿里云镜像仓库”时，默认执行本流程。
- 默认目标：`ssh ecs-main`（`root@47.116.180.173`），先跑 `hostname && uname -a` 验证连通。
- 默认镜像仓库：
  - Registry: `crpi-02k3y8iudey5q0vb.cn-shanghai.personal.cr.aliyuncs.com`
  - Repo: `mirror_ns/tool_box`
  - Tag: 用户给定版本号（若未给，先询问）。
- 默认公网入口：`47.116.180.173`。
- 部署目录统一：`/srv/apps/tool-box-public`（`compose.yaml` + `.env.runtime`）。
- 编排兼容：
  - 优先 `docker compose`
  - 若 ECS 仅有 v1，回退 `docker-compose`

## 本机发布步骤（必须按序）

1. `docker build -t <registry/repo:tag> .`
2. `docker login --username=MjnnAliCloud <registry>`（优先 `--password-stdin`，禁止回显明文密码）
3. `docker push <registry/repo:tag>`
4. 若用户提到“部署机包/离线包”，追加执行：
   - `docker save -o release/tool_box_<tag>_ecs.tar <image>`
   - 输出 SHA256 校验值。

## ECS 部署步骤（必须按序）

1. ECS 上执行 `docker login` 与 `docker pull <image>`（若主机已持久登录镜像仓库，可用 **`SKIP_DOCKER_LOGIN=1`** 调用 `scripts/ecs-deploy-public.sh` 以支持非交互 SSH；或设置 **`REGISTRY_PASSWORD`** 走 `--password-stdin`）
2. 在 `/srv/apps/tool-box-public` 生成或更新：
   - `.env.runtime`
   - `compose.yaml`
3. 启动容器（或替换旧容器）时必须带：
   - `DATABASE_URL=<云端PGSQL连接串>`
   - `TOOLBOX_EXTERNAL_PUBLIC_IP=47.116.180.173`
   - `TOOLBOX_VISIBLE_TOOL_KEYS=service-id-registry`
   - `TOOLBOX_WORKERS=2`
   - `SQLALCHEMY_POOL_SIZE=12`
   - `SQLALCHEMY_MAX_OVERFLOW=8`
   - `SQLALCHEMY_POOL_TIMEOUT=45`
   - `SQLALCHEMY_POOL_RECYCLE=1800`
   - `SQLALCHEMY_STATEMENT_TIMEOUT_MS=15000`
   - `BACKEND_CORS_ORIGINS=["http://47.116.180.173","https://47.116.180.173","http://localhost","http://127.0.0.1"]`
   - `TOOLBOX_VERSION=<版本号>`（默认可用镜像 tag）
   - `TOOLBOX_SPEC_REVISION=<规格修订，可空>`
   - `TOOLBOX_VERSION_TITLE=版本更新`
   - `TOOLBOX_CHANGELOG=<本次变更说明>`
4. 用 compose 启动（`up -d --remove-orphans`），不要继续手工 `docker run`
5. 健康检查：`curl http://127.0.0.1:<port>/health`，并返回 `docker ps` 与关键日志
6. 版本接口校验（必须）：
   - `curl http://127.0.0.1:<port>/api/v1/meta/version`
   - 期望返回 `version/spec_revision/title/changelog`，且与本次部署目标一致。
7. 外网工具可见性校验（必须）：
   - `curl -H 'Host: 47.116.180.173' http://127.0.0.1:<port>/api/v1/tools/?skip=0&limit=10`
   - 期望仅返回 `service-id-registry`。

## 版本管理治理规则（必须遵守）

- 管理端后续统一使用“版本管理 -> 从工具接口同步版本”。
- 禁止再走“手工发版录入”的旧流程（已迁移为工具自描述 + 宿主记录）。
- 若用户提到“发版后让管理员/负责人看到版本变更”，默认动作是：
  1) 部署时写入版本环境变量；
  2) 校验 `/api/v1/meta/version`；
  3) 提醒在管理页执行同步并保存版本历史。

## 失败兜底（必须执行）

- 若健康检查失败且日志卡在 `Waiting for application startup`，优先排查 ECS 到 RDS 5432 连通性。
- 快速检查：`timeout 8 bash -lc 'cat < /dev/null > /dev/tcp/<rds-host>/5432'`
- 若不通，明确提示用户先放通 RDS 白名单/安全组，再重试部署。

